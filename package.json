{
  "name": "achievement-tool-maker",
  "version": "0.1.0",
  "module": "src/index.ts",

  "scripts": {
    "start": "bun run src/index.ts",
    "test": "cd test && bun wiptest"
  },

  "devDependencies": {
    "bun-types": "latest"
  }
}
